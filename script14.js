var a = 2;
var b = 3;
var c = -9;

function calculateDiscriminant(a, b, c, discrim) {
    
    return discrim = b * b - 4 * a * c;
}
function calculateRoots1(x, x1, x2) {
    if (discrim < 0) {
        return x = " Нет действительных корней";
    } else if (discrim = 0) {
        return x = -b / 2 * a;
        
     } else (discrim > 0); {
         return x1,x2 = -b +- Math.sqrt(discrim) / 2 * a;
        
     }
}

// && x2 = -b - Math.sqrt(discrim) / 2 * a;



    
    
